#include <iostream>
int main(){
    std::ios::sync_with_stdio(false);
    std::cin.tie(NULL);
    std::cout.tie(NULL);

    int TC;
    long int N,M;
    std::cin >> TC;
    while(TC--)
    {
        std::cin >> N >> M;
        while(M--)
        {
            
        }
    }
   


    return 0;
}